{% extends 'base.html.twig' %}

{% block title %}SubDomain{% endblock %}

{% block body %}
    <h1>SubDomain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_domain.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_domain.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sub_domain_index') }}">back to list</a>

    <a href="{{ path('sub_domain_edit', {'id': sub_domain.id}) }}">edit</a>

    {{ include('sub_domain/_delete_form.html.twig') }}
{% endblock %}
